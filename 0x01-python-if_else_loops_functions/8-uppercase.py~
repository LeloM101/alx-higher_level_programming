#!/usr/bin/python3
def uppercase(str):
    result = ''
    '''prints a string in uppercase followed by a new line'''
        for char in str:
            result += chr(ord(char) -32)
        return result
        else:
            print("{}".format(chr(str)))
